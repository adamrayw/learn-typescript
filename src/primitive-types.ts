// There are 7 primitive in JS:
// string, number, bigInt, boolean, undefined, null and symbol

// Explicit type annotation
let fistName: string = 'Adam'

// if we assign a value (as above), we don't need to state the type - TS will infer it ("Implicit type annotation")
let firstName = 'Adam'